#ifndef UTILS_H
#define UTILS_H

#include <cstdlib>
#include <cmath>

inline void initGraph(float* matrix, int n) {
    // TODO: Initialize graph with random weights
}

inline bool verifyResults(float* cpu_res, float* gpu_res, int n) {
    // TODO: Compare matrices
    return true;
}

#endif // UTILS_H
